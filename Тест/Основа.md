```
Как называется единица данных передаваемая на Tранспортном уровне модели TCP/IP?
1.Фрейм
2.Сегмент
3.Пакет
4.Байт

Сколько уровней в сетевой модели TCP/IP?
1.7
2.5
3.4
4.6
Деинкапсуляция это
процесс передачи пакетов
процесс восстановления данных после потерь
процесс добавления заголовка (иногда и завершителя)
процесс сбрасывания заголовка (иногда и завершителя)

Взаимодействие смежных уровней это
когда нижестоящий уровень одного устройства предоставляет сервис вышестоящему уровню другого устройства
когда вышестоящий уровень предоставляет сервис нижестоящему
тоже самое что и одноуровневое взаимодействие
когда в пределах одного устройства нижестоящий уровень сетевой модели предоставляет сервис вышестоящему

К какому уровню сетевой модели TCP/IP относится ваш браузер?
1.к Сетевому уровню
2.к уровню Сессий
3.к Транспортному уровню 
4. ни к какому

За счет чего происходит одноуровневое взаимодействие?
1.за ваш счёт
2.за счёт IP адресов
3.за счёт заголовков
4.нет правильного ответа

На каком уровне сетевой модели TCP/IP определяется ip адресация и группировка ip адресов?
1.На Канальном уровне (Физический уровень)
2.На Сетевом Уровне (уровень Интернет)
3.На Транспортном Уровне
4.Эти функции не определяются сетевой моделью TCP/IP

Как называется единица данных передаваемая на Сетевом уровне модели TCP/IP?
Байт
Фрейм
Пакет
Сегмент

К каким данным прикрепляется заголовок Сетевого уровня (уровня Интернет)?
К сегменту, сформированному на уровне Приложений
Этот заголовок добавляется на каждом уровне
К сегменту, сформированному на Транспортном уровне
К данным, сформированным на уровне Приложений

Какое утверждение верно?
Все устройства обмениваются данными по сети, используя сетевую модель TCP/IP
True = False
Все устройства обмениваются данными по сети, используя сетевую модель OSI
Все устройства обмениваются данными по сети, используя сетевые модели TCP/IP и OSI

К какому уровню сетевой модели TCP/IP относится протокол HTTP?
Транспортный Уровень
Физический Уровень
Уровень Приложений
Сетевой Уровень

Как называется единица данных передаваемая на Физическом (Канальном) уровне модели TCP/IP?
Сегмент
Фрейм
Пакет
Тапок

На каком уровне сетевой модели TCP/IP нет заголовка?
На Уровне Приложений
На Физическом Уровне
Заголовок есть на каждом уровне
На Сетевом Уровне

На каком уровне сетевой модели TCP/IP осуществляется восстановление данных после потери?
На Сетевом Уровне
На Физическом Уровне
На Уровне Приложений
На Транспортном Уровне

К какому уровню сетевой модели TCP/IP относится протокол TCP?
Транспортный Уровень
Физический Уровень
Уровень Приложений
Сетевой Уровень

Маршрутизация это
передача пакетов Транспортного Уровня
процесс прокладки машрута между роутерами
ориентирование на местности
процесс передачи пакетов

К какому уровню сетевой модели TCP/IP относится протокол Ethernet?
к уровню Интернет (Сетевому уровню)
к Транспортному уровню
Ethernet отдельный стандарт, который не относится к сетевой модели TCP/IP
к Физическому уровню (Канальному уровню)

Какое действие происходит на Физическом уровне модели TCP/IP?
сегмент инкапсулируется в фрейм и начинается передача по физической среде
Физический Уровень передает данные на Транспортный уровень
пакет инкапсулируется в фрейм и начинается передача по физической среде
прежде чем передать данные на Транспортный уровень, к пакету добавляется завершитель

```
